<Page
    x:Class="AxioVital.Desktop.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#0A658E">

    <Grid Padding="30">
        <!-- 1. Top Left Header Brand -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Spacing="10">
            <Grid Width="26" Height="26">
                <Ellipse Fill="White" />
                <FontIcon Glyph="&#xE774;" FontSize="18" Foreground="#0A658E" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
            <TextBlock Text="AxioVital" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 2. Main Center Content -->
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-40,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Large Title -->
            <TextBlock
                Grid.Row="0"
                Text="AxioVital Environment ™"
                Foreground="#82B2C9"
                FontSize="38"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"
                Margin="0,0,0,45" />

            <!-- Login Form Controls Container -->
            <StackPanel Grid.Row="1" Width="280" HorizontalAlignment="Center">

                <!-- User Name Field -->
                <TextBlock Text="User Name :" Foreground="White" FontSize="13" FontWeight="Normal" Margin="0,0,0,5" />
                <ComboBox
                    ItemsSource="{Binding Users}"
                    SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                    Width="280"
                    Height="32"
                    HorizontalAlignment="Stretch"
                    Background="White"
                    Foreground="Black"
                    CornerRadius="0"
                    BorderBrush="#777777"
                    Margin="0,0,0,14" />

                <!-- Password Field -->
                <TextBlock Text="Password :" Foreground="White" FontSize="13" FontWeight="Normal" Margin="0,0,0,5" />
                <PasswordBox
                    x:Name="PasswordInput"
                    PasswordChanged="PasswordInput_PasswordChanged"
                    Width="280"
                    Height="32"
                    HorizontalAlignment="Stretch"
                    Background="White"
                    Foreground="Black"
                    CornerRadius="0"
                    BorderBrush="#777777"
                    Margin="0,0,0,14" />

                <!-- Domain Field -->
                <TextBlock Text="Domain :" Foreground="White" FontSize="13" FontWeight="Normal" Margin="0,0,0,5" />
                <ComboBox
                    ItemsSource="{Binding Domains}"
                    SelectedItem="{Binding SelectedDomain, Mode=TwoWay}"
                    Width="280"
                    Height="32"
                    HorizontalAlignment="Stretch"
                    Background="White"
                    Foreground="Black"
                    CornerRadius="0"
                    BorderBrush="#777777"
                    Margin="0,0,0,22" />

                <!-- Status / Error Message -->
                <TextBlock
                    Text="{Binding StatusMessage}"
                    Foreground="#FF8888"
                    FontSize="11"
                    HorizontalAlignment="Center"
                    TextWrapping="Wrap"
                    Margin="0,0,0,10"
                    Visibility="{Binding HasError}" />

                <!-- Action Buttons Row -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                    <Button
                        Content="OK"
                        Command="{Binding LoginCommand}"
                        Width="100"
                        Height="30"
                        Padding="0"
                        Background="#D5D5D5"
                        Foreground="Black"
                        BorderBrush="#777777"
                        BorderThickness="1"
                        CornerRadius="0"
                        FontSize="12"
                        FontWeight="Normal" />

                    <Button
                        Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Width="100"
                        Height="30"
                        Padding="0"
                        Background="#D5D5D5"
                        Foreground="Black"
                        BorderBrush="#777777"
                        BorderThickness="1"
                        CornerRadius="0"
                        FontSize="12"
                        FontWeight="Normal" />
                </StackPanel>

            </StackPanel>
        </Grid>

        <!-- 3. Bottom Left Footer Section -->
        <Grid VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Bottom Left Circle Badge ("N") -->
            <Grid Grid.Column="0" VerticalAlignment="Bottom" Margin="0,0,12,0">
                <Ellipse Width="34" Height="34" Fill="#111111" />
                <TextBlock Text="N" Foreground="White" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>

            <!-- Footer Text Stack -->
            <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Spacing="2" MaxWidth="850">
                <TextBlock Text="Operating Environment" Foreground="White" FontSize="14" FontWeight="Bold" />
                <TextBlock Text="© 2026 AxioVital Corporation. All rights reserved." Foreground="#82B2C9" FontSize="11" />
                <TextBlock Text="Access and use of this solution system (including components thereof) require, and are governed by, license(s) from AxioVital Corporation." Foreground="#73A3BA" FontSize="10" TextWrapping="Wrap" />
                <TextBlock Text="Unauthorized use, access, reproduction, display or distribution of any portion of this solution or the data contained therein may result in severe civil damages and criminal penalties. Further Information may be found in Help/About." Foreground="#73A3BA" FontSize="10" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
