<UserControl
    x:Class="AxioVital.Desktop.Views.CustomisedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="EhrRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="#000000" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="7,0,0,0" />
            <Setter Property="MinHeight" Value="20" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent" Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Foreground" Value="#000000" />
                                            <Setter Target="OuterCircle.Stroke" Value="#005C8A" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Foreground" Value="#000000" />
                                            <Setter Target="OuterCircle.Stroke" Value="#005C8A" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Foreground" Value="#888888" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="OuterCircle.Stroke" Value="#005C8A" />
                                            <Setter Target="InnerDot.Visibility" Value="Visible" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked">
                                        <VisualState.Setters>
                                            <Setter Target="OuterCircle.Stroke" Value="#777777" />
                                            <Setter Target="InnerDot.Visibility" Value="Collapsed" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Indeterminate" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <!-- Radio Circle Glyph -->
                            <Grid Grid.Column="0" Width="14" Height="14" VerticalAlignment="Center">
                                <Ellipse x:Name="OuterCircle" Width="14" Height="14" Stroke="#777777" StrokeThickness="1.5" Fill="White" />
                                <Ellipse x:Name="InnerDot" Width="6" Height="6" Fill="#005C8A" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>

                            <!-- Content Presenter Text -->
                            <ContentPresenter x:Name="ContentPresenter"
                                              Grid.Column="1"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Foreground="#000000"
                                              FontSize="{TemplateBinding FontSize}"
                                              FontFamily="{TemplateBinding FontFamily}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <!-- Top Toolbar -->
            <RowDefinition Height="Auto" />
            <!-- Main Content Card -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Tool Icons Bar -->
        <Grid Grid.Row="0" Background="#FAFAFA" Padding="12,5" BorderBrush="#E2EAF0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE71E;" FontSize="11" Foreground="#555555" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE74E;" FontSize="11" Foreground="#555555" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE749;" FontSize="11" Foreground="#555555" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE71E;" FontSize="11" Foreground="#555555" VerticalAlignment="Center" />
                    <TextBlock Text="100% ▾" FontSize="10.5" Foreground="#333333" FontFamily="Segoe UI" VerticalAlignment="Center" />
                </StackPanel>
                <FontIcon Glyph="&#xE718;" FontSize="11" Foreground="#555555" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE80F;" FontSize="11" Foreground="#D97706" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Main Card Border -->
        <Border Grid.Row="1" BorderBrush="#CBD5E1" BorderThickness="1" Margin="10" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <!-- Left Panel: My Default Organizer View -->
                    <ColumnDefinition Width="230" />
                    <!-- Right Panel: My MPages Selection -->
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Panel -->
                <Border Grid.Column="0" BorderBrush="#CBD5E1" BorderThickness="0,0,1,0" Padding="14,14,14,14">
                    <StackPanel Spacing="4">
                        <TextBlock Text="My Default Organizer View" Foreground="#0A527B" FontSize="11.5" FontWeight="Bold" Margin="0,0,0,8" FontFamily="Segoe UI" />

                        <RadioButton Content="Message Centre" IsChecked="True" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="Patient Overview" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="Ambulatory Organizer" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="MyExperience" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="Patient List" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="Dynamic Worklist" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                        <RadioButton Content="LearningLIVE" Style="{StaticResource EhrRadioButtonStyle}" GroupName="DefaultOrganizerGroup" />
                    </StackPanel>
                </Border>

                <!-- Right Panel -->
                <Border Grid.Column="1" Padding="16,14,16,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Header Info -->
                        <StackPanel Grid.Row="0" Spacing="2" Margin="0,0,0,10">
                            <TextBlock Text="My MPages Selection" Foreground="#0A527B" FontSize="11.5" FontWeight="Bold" FontFamily="Segoe UI" />
                            <TextBlock Text="For Tab: Provider View" Foreground="#333333" FontSize="10.5" FontFamily="Segoe UI" />
                            <TextBlock Text="For Role: Provider" Foreground="#333333" FontSize="10.5" FontFamily="Segoe UI" />
                        </StackPanel>

                        <!-- Radio Options ScrollViewer -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <StackPanel Spacing="3">
                                <RadioButton Content="Anesthesiologist Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Cardiology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Critical Care Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Dermatology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Endocrinology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Gastroenterology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="General Medicine Workflow" IsChecked="True" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="General Surgery Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Gerontology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Infectious Disease Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Laboratory Medicine Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Medical Microbiologist Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Mental Health Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Nephrology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Neurology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Neurosurgery Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Oncology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Ophthalmology Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                                <RadioButton Content="Oral and Maxillofacial Surgery Workflow" Style="{StaticResource EhrRadioButtonStyle}" GroupName="MPagesGroup" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
