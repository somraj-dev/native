<UserControl
    x:Class="AxioVital.Desktop.Views.LabsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="Segoe UI">

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <!-- 1. Action Toolbar -->
            <RowDefinition Height="Auto" />
            <!-- 2. Table Header -->
            <RowDefinition Height="Auto" />
            <!-- 3. Table Data Rows -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Action Toolbar matching Reference Image 1:1 -->
        <Grid Grid.Row="0" Background="#F0F4F8" Padding="14,5,14,5" BorderBrush="#CBD7DF" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="22" VerticalAlignment="Center">
                <!-- Communicate ▾ -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE789;" FontSize="11" Foreground="#C82333" />
                    <TextBlock Text="Communicate ▾" Foreground="#222222" FontSize="11" FontWeight="Normal" />
                </StackPanel>

                <!-- Open -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8B7;" FontSize="11" Foreground="#E68A00" />
                    <TextBlock Text="Open" Foreground="#222222" FontSize="11" />
                </StackPanel>

                <!-- Message Journal -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8A5;" FontSize="11" Foreground="#0A658E" />
                    <TextBlock Text="Message Journal" Foreground="#222222" FontSize="11" />
                </StackPanel>

                <!-- Forward Only -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE72A;" FontSize="11" Foreground="#E65100" />
                    <TextBlock Text="Forward Only" Foreground="#222222" FontSize="11" />
                </StackPanel>

                <!-- Select Patient -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE77B;" FontSize="11" Foreground="#0A658E" />
                    <TextBlock Text="Select Patient" Foreground="#222222" FontSize="11" />
                </StackPanel>

                <!-- Select All -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE73D;" FontSize="11" Foreground="#111111" />
                    <TextBlock Text="Select All" Foreground="#222222" FontSize="11" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Data Table Header Bar -->
        <Grid Grid.Row="1" Background="#EDF3F7" Padding="12,5,12,5" BorderBrush="#CBD7DF" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" MinWidth="220" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Patient Name" Foreground="#334455" FontSize="10" FontWeight="Bold" />
            <TextBlock Grid.Column="1" Text="Plan Name" Foreground="#334455" FontSize="10" FontWeight="Bold" />
            <TextBlock Grid.Column="2" Text="Action" Foreground="#334455" FontSize="10" FontWeight="Bold" />
            <TextBlock Grid.Column="3" Text="Details" Foreground="#334455" FontSize="10" FontWeight="Bold" />
            <TextBlock Grid.Column="4" Text="Details" Foreground="#334455" FontSize="10" FontWeight="Bold" />
            <TextBlock Grid.Column="5" Text="Status" Foreground="#334455" FontSize="10" FontWeight="Bold" />
        </Grid>

        <!-- Data Table Items -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="LabsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding RowBackground}" Padding="12,4,12,4" BorderBrush="#EBF1F5" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="280" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" MinWidth="220" />
                                    <ColumnDefinition Width="120" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding PatientName}" Foreground="#111111" FontSize="10" FontWeight="Bold" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding PlanName}" Foreground="#0A658E" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding Action}" Foreground="#222222" FontSize="9.5" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{Binding DetailsDate}" Foreground="#556677" FontSize="9.5" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="4" Text="{Binding DetailsNotes}" Foreground="#222222" FontSize="9.5" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="5" Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>