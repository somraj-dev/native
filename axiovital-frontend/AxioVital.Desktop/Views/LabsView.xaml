<UserControl
    x:Class="AxioVital.Desktop.Views.LabsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <!-- Action Toolbar -->
            <RowDefinition Height="Auto" />
            <!-- Table Header -->
            <RowDefinition Height="Auto" />
            <!-- Table Data Rows -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Action Toolbar matching Reference Image 1:1 -->
        <Grid Grid.Row="0" Background="#F4F6F9" Padding="14,6,14,6" BorderBrush="#CBD0D8" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="22" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE789;" FontSize="11" Foreground="#C82333" />
                    <TextBlock Text="Communicate" Foreground="#333333" FontSize="11" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8B7;" FontSize="11" Foreground="#D97706" />
                    <TextBlock Text="Open" Foreground="#333333" FontSize="11" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8A5;" FontSize="11" Foreground="#0A658E" />
                    <TextBlock Text="Message Journal" Foreground="#333333" FontSize="11" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE72A;" FontSize="11" Foreground="#C82333" />
                    <TextBlock Text="Forward Only" Foreground="#333333" FontSize="11" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE77B;" FontSize="11" Foreground="#0A658E" />
                    <TextBlock Text="Select Patient" Foreground="#333333" FontSize="11" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE73D;" FontSize="11" Foreground="#0A658E" />
                    <TextBlock Text="Select All" Foreground="#333333" FontSize="11" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Data Table Header Bar -->
        <Grid Grid.Row="1" Background="#E2EAF0" Padding="12,5,12,5" BorderBrush="#B5C4D0" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="290" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Patient Name" Foreground="#111111" FontSize="11" FontWeight="SemiBold" />
            <TextBlock Grid.Column="1" Text="Plan Name" Foreground="#111111" FontSize="11" FontWeight="SemiBold" />
            <TextBlock Grid.Column="2" Text="Action" Foreground="#111111" FontSize="11" FontWeight="SemiBold" />
            <TextBlock Grid.Column="3" Text="Details" Foreground="#111111" FontSize="11" FontWeight="SemiBold" />
            <TextBlock Grid.Column="4" Text="Details" Foreground="#111111" FontSize="11" FontWeight="SemiBold" />
            <TextBlock Grid.Column="5" Text="Status" Foreground="#111111" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,24,0" />
        </Grid>

        <!-- Data Table Items -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="LabsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding RowBackground}" Padding="12,5,12,5" BorderBrush="#EAEAEA" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="250" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="130" />
                                    <ColumnDefinition Width="290" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding PatientName}" Foreground="Black" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding PlanName}" Foreground="#0066CC" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding Action}" Foreground="#333333" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{Binding DetailsDate}" Foreground="#666666" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="4" Text="{Binding DetailsNotes}" Foreground="#333333" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="5" Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,24,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>