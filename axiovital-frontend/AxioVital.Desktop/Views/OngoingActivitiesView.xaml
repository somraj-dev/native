<UserControl
    x:Class="AxioVital.Desktop.Views.OngoingActivitiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="Segoe UI">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <!-- Sub-header Toolbar -->
            <RowDefinition Height="Auto" />
            <!-- Main Data Table -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Filter & Action Bar -->
        <Grid Grid.Row="0" Background="#FFFFFF" Padding="12,5" BorderBrush="#E2EAF0" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left: Patient List Filter Dropdown -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="Patient List:" Foreground="#333333" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                <ComboBox SelectedIndex="0" Height="24" Width="220" FontSize="11" Padding="8,2" CornerRadius="2" VerticalAlignment="Center">
                    <ComboBoxItem Content="Holding In Recovery Room" />
                    <ComboBoxItem Content="All Inpatient Units" />
                    <ComboBoxItem Content="Emergency Department" />
                </ComboBox>
            </StackPanel>

            <!-- Right: Physician Contact Links & Icons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="Physician Contact" Foreground="#0A658E" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                <TextBlock Text="|" Foreground="#CCCCCC" FontSize="11" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE77B;" FontSize="12" Foreground="#333333" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE749;" FontSize="12" Foreground="#333333" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Scrollable Data Table -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="22" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Table Column Headers -->
                <Grid Grid.Row="0" Background="#E2EAF0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="480" />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="250" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" BorderBrush="#C2D3E0" BorderThickness="0,0,1,1" Padding="6,2">
                        <TextBlock Text="Location" Foreground="#334455" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center" />
                    </Border>
                    <Border Grid.Column="1" BorderBrush="#C2D3E0" BorderThickness="0,0,1,1" Padding="6,2">
                        <TextBlock Text="Patient" Foreground="#334455" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center" />
                    </Border>
                    <Border Grid.Column="2" BorderBrush="#C2D3E0" BorderThickness="0,0,1,1" Padding="2,2">
                        <TextBlock Text="" />
                    </Border>
                    <Border Grid.Column="3" BorderBrush="#C2D3E0" BorderThickness="0,0,1,1" Padding="6,2">
                        <TextBlock Text="Physician Contact" Foreground="#334455" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center" />
                    </Border>
                    <Border Grid.Column="4" BorderBrush="#C2D3E0" BorderThickness="0,0,0,1" Padding="6,2">
                        <TextBlock Text="Diagnosis" Foreground="#334455" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center" />
                    </Border>
                </Grid>

                <!-- Data Rows -->
                <ItemsControl Grid.Row="1" x:Name="OngoingActivitiesItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding RowBackground}" BorderBrush="#E5EAEF" BorderThickness="0,0,0,1">
                                <Grid Height="24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="480" />
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="250" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Location -->
                                    <Border Grid.Column="0" BorderBrush="#E2E6EA" BorderThickness="0,0,1,0" Padding="6,2">
                                        <TextBlock Text="{Binding Location}" Foreground="#555555" FontSize="10.5" FontFamily="Segoe UI" VerticalAlignment="Center" />
                                    </Border>

                                    <!-- Patient Name & Info -->
                                    <Border Grid.Column="1" BorderBrush="#E2E6EA" BorderThickness="0,0,1,0" Padding="6,2">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding PatientName}" Foreground="#000000" FontWeight="Bold" FontSize="10.5" FontFamily="Segoe UI" VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding PatientSubInfo}" Foreground="#888888" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Border>

                                    <!-- Expander Arrow -->
                                    <Border Grid.Column="2" BorderBrush="#E2E6EA" BorderThickness="0,0,1,0" Padding="2,2">
                                        <TextBlock Text="▶" Foreground="#CCCCCC" FontSize="8" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>

                                    <!-- Physician Contact -->
                                    <Border Grid.Column="3" BorderBrush="#E2E6EA" BorderThickness="0,0,1,0" Padding="6,2">
                                        <TextBlock Text="{Binding PhysicianContact}" Foreground="#0A658E" FontSize="10.5" FontFamily="Segoe UI" VerticalAlignment="Center" />
                                    </Border>

                                    <!-- Diagnosis -->
                                    <Border Grid.Column="4" BorderBrush="#E2E6EA" BorderThickness="0,0,0,0" Padding="6,2">
                                        <TextBlock Text="{Binding Diagnosis}" Foreground="#0A658E" FontSize="10.5" FontFamily="Segoe UI" VerticalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
